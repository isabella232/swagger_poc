import { Enumerable as IEnumerable } from 'linq-es2015';
export declare class Installer {
    private static ensureExists(dir);
    private static GetFallbackUrl(url);
    static readonly RootFolder: string;
    static readonly FrameworkFolder: string;
    static readonly PluginsFolder: string;
    static readonly AutorestFolder: string;
    static readonly LatestAutorestVersion: string;
    static readonly InstalledAutorestVersions: IEnumerable<string>;
    static readonly InstalledFrameworkVersions: IEnumerable<string>;
    static readonly LatestFrameworkVersion: string;
    static readonly dotnet: string;
    private static HttpGet(url, filename, targetFolder, resolve, reject);
    static InstallFramework(): Promise<string>;
    static InstallAutoRest(version: string): Promise<string>;
    private static GetVersionsInFolder(path);
}
